[tool.uv.workspace]
members = ["backend"]

[tool.basedpyright]
typeCheckingMode = "recommended"
pythonVersion = "3.11"
venvPath = "."
venv = ".venv"
extraPaths = ["backend"]

[tool.typos.files]
extend-exclude = [
    "uv.lock",
]
